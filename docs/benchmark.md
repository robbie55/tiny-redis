# Benchmark methodology and results

<!--
TODO(robbie): the README's headline number links here, so this page decides whether that
number means anything.

Rules:
  - Every number traces to a raw run committed under bench/results/.
  - Median of N trials after warmup. Report p50 and p99, never a mean.
  - State hardware and compiler flags. Regenerate docs/hardware.txt with
    `./scripts/dev.sh hw` on the machine that produced the numbers.
  - If the host is virtualized, say so.

Leave the sections below empty until there's a real run. An empty table is honest, and a
placeholder number isn't.
-->

## Hardware

See [`hardware.txt`](hardware.txt), generated by `./scripts/dev.sh hw`.

TODO(robbie): CPU, core count, memory, kernel, compiler, and whether the host is
virtualized.

## Method

TODO(robbie): enough detail for someone else to reproduce the run.

- Build preset and exact flags (`release` = `-O3 -DNDEBUG`)
- Trial count, warmup iterations, outlier-rejection rule
- Client: `redis-benchmark` and/or `tinyredis_bench` invocation, verbatim
- Where the client ran relative to the server, e.g. same host over loopback, pinned to
  which cores, and why
- Key/value sizes, key-space size, connection count, pipeline depth

## Results

TODO(robbie): at minimum command, pipeline depth, clients, ops/sec, p50, p99, and the
results file each row came from.

| command | pipeline | clients | ops/sec | p50 (ms) | p99 (ms) | source |
| ------- | -------- | ------- | ------- | -------- | -------- | ------ |
|         |          |         |         |          |          |        |

## Headline number

TODO(robbie): the one figure the README quotes, and the configuration that produced it.

## Caveats

TODO(robbie): what this measurement does and doesn't show.
