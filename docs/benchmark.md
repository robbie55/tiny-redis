# Benchmark methodology and results

<!--
TODO(robbie): this file is a deliverable, not a scratch pad. The README's headline
number links here, and an interviewer who opens the repo will read this page to decide
whether the number means anything.

Rules, non-negotiable:
  - Every number here traces to a raw run committed under bench/results/.
  - Median of N trials after warmup. Report p50 and p99. Never a mean.
  - State the hardware and the compiler flags. docs/hardware.txt is generated by
    `./scripts/dev.sh hw` -- regenerate it on the machine that produced the numbers.
  - If the host is virtualized, say so plainly. An honest caveat costs nothing; an
    unstated one is the thing that gets caught.

Fill the sections below during T-13. Leave them empty until then -- an empty table is
honest, a placeholder number is not.
-->

## Hardware

See [`hardware.txt`](hardware.txt), generated by `./scripts/dev.sh hw`.

TODO(robbie): summarize in prose here. CPU, core count, memory, kernel, compiler,
and whether the host is virtualized.

## Method

TODO(robbie): record, in enough detail that someone else could reproduce the run.

- Build preset and exact flags (`release` = `-O3 -DNDEBUG`)
- Trial count, warmup iterations, outlier-rejection rule
- Client: `redis-benchmark` invocation and/or `tinyredis_bench` invocation, verbatim
- Where the client ran relative to the server (same host over loopback? pinned to which
  cores?) and why that choice
- Key/value sizes, key-space size, connection count, pipeline depth

## Results

TODO(robbie): table. Columns at minimum: command, pipeline depth, clients, ops/sec,
p50, p99, and the results file each row came from.

| command | pipeline | clients | ops/sec | p50 (ms) | p99 (ms) | source |
| ------- | -------- | ------- | ------- | -------- | -------- | ------ |
|         |          |         |         |          |          |        |

## Headline number

TODO(robbie): the single figure that goes in the README and on the résumé, with one
sentence naming the configuration it came from.

## Caveats

TODO(robbie): what this measurement does and does not show. Being explicit here is
what makes the number defensible.
